# Web Space Station — environment variables (template)
# Copy to `.env` and fill in real values. Do NOT commit `.env`.

# PostgreSQL connection string used by Prisma.
# Local example:
DATABASE_URL="postgresql://postgres:@db.blpnxcnapirdhjtujimo.supabase.co:5432/postgres"

# Supabase Auth (public — safe to expose to the browser).
# Project Settings → API → Project URL / anon public key.
NEXT_PUBLIC_SUPABASE_URL="https://blpnxcnapirdhjtujimo.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-public-key"
